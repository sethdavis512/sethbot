import { type DispatchWithoutAction, useReducer } from 'react';

export default function use{{ pascalCase name }}(initialState: boolean = false): [boolean, DispatchWithoutAction] {
    return useReducer((s) => !s, initialState);
}
