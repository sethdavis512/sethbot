import { useCallback, useReducer } from 'react';

export default function use{{ pascalCase name }}(initialState: boolean = false) {
    const [toggleState, setToggleState] = useReducer<boolean>((s) => !s, false);

    return [toggleState, setToggleState];
}
