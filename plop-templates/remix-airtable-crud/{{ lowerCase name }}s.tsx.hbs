import { Outlet } from "@remix-run/react";

export default function {{ pascalCase name }}sRoute() {
    return (
        <div className="max-w-5xl mx-auto">
            <h1 className="text-4xl font-bold">{{ sentenceCase name }}s</h1>
            <Outlet />
        </div>
    );
}
